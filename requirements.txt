# Pinned for faster, reproducible Render builds.
# Install CPU torch first in build: pip install torch --index-url https://download.pytorch.org/whl/cpu
streamlit==1.58.0
python-dotenv==1.2.2
supabase==2.31.0
groq==1.5.0
cohere==5.15.0
numpy==2.2.6
pymupdf==1.25.5
python-docx==1.1.2
sentence-transformers==3.4.1
